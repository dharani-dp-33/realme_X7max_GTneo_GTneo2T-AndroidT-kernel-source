- name: Install Dependencies
  run: |
    sudo apt-get update -y
    sudo apt-get install -y \
      build-essential \
      libncurses6-dev \  # Updated package name
      bison \
      flex \
      libssl-dev \
      findutils \
      git
